@model IEnumerable<EsibayeniSolution.Models.MaintainanceProcess>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MainName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MainName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MainPId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MainPId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MainPId })
        </td>
    </tr>
}

</table>
